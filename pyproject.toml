[project]
name = "trakt-calendar"
version = "0.1.0"
description = "Sync Trakt TV shows and movies release dates to iCal and Google Calendar"
requires-python = ">=3.12"
dependencies = [
    "google-api-python-client>=2.150.0",
    "google-auth-httplib2>=0.2.0",
    "google-auth-oauthlib>=1.2.0",
    "icalendar>=6.0.0",
    "python-dateutil>=2.9.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
]

[dependency-groups]
dev = [
    "mypy>=1.11.0",
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.6.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = [
    "E",     # pycodestyle errors
    "W",     # pycodestyle warnings
    "F",     # Pyflakes
    "I",     # isort
    "B",     # flake8-bugbear
    "UP",    # pyupgrade
    "SIM",   # flake8-simplify
    "RUF",   # Ruff-specific rules
]
ignore = [
    "E501",   # Line too long (handled by formatter)
    "BLE001", # Blind exception catching (needed for resilient Trakt/Google API fallbacks)
    "B904",   # Exception chaining (clean domain exceptions for calendar sync)
]

[tool.ruff.lint.isort]
known-first-party = ["auth", "generate_ical", "google_sync", "ical_builder", "trakt_api"]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
warn_unused_configs = true
disallow_untyped_defs = false
check_untyped_defs = true
